# Configuração para o Firebase entender o Next.js
runtime: nodejs20 # Recomendo a 20 para o Next 14

runConfig:
  concurrency: 80
  cpu: 1
  memoryMiB: 512

# Força o Firebase a olhar para a pasta atual e não para a subpasta fantasma
app:
  rootDirectory: .