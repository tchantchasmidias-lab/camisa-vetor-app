kind: "AppStack"
apiVersion: "v1"
runConfig:
  concurrency: 80
  cpu: 1
  memoryMiB: 512
# A chave foi removida daqui por segurança (GitGuardian alert).
# A variável GOOGLE_GENAI_API_KEY deve ser configurada diretamente no console do Firebase App Hosting.